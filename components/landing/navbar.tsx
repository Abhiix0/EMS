/**
 * Landing navbar adapter.
 * Re-exports the authoritative LandingNavbar implementation without modifying it.
 */
export {
  LandingNavbar,
  LandingNavbar as Navbar,
  default,
} from "@/components/landing-navbar";
